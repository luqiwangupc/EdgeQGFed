\begin{abstract}
边缘智能任务通常产生于大量分散终端，受限于带宽、算力、能耗和连接稳定性，难以直接采用传统端侧联邦训练；同时，边缘数据常具有 non-IID 分布和少标签特征。为此，本文提出 EdgeQGFed，一种面向通信受限边缘网络的云-边-端协同学习框架。该框架将主要训练负载上移至边缘服务器，利用云端图注意力机制对边缘节点间的语义关系和更新质量进行建模，并结合置信度加权伪标签学习缓解少标签训练问题。进一步地，EdgeQGFed 引入资源感知客户端采样和三层通信时延模型，以刻画带宽异构、客户端掉线和通信预算约束下的训练效率。实验将从模型精度、通信开销、time-to-accuracy 和鲁棒性等方面验证所提方法在跨模态边缘智能任务中的有效性。
\end{abstract}

\section{预备知识与问题定义}
\label{sec:problem}

\subsection{云-边-端协同学习系统}
\label{subsec:system}

本文考虑一个云-边-端协同学习系统，其中包含一个云服务器、一组边缘服务器
$\mathcal{E}=\{1,\ldots,E\}$，以及一组客户端设备
$\mathcal{C}=\{1,\ldots,C\}$。每个客户端 $i$ 关联到一个边缘服务器
$e(i)\in\mathcal{E}$。客户端 $i$ 拥有本地数据集
$\mathcal{D}_i=\mathcal{D}_i^l\cup\mathcal{D}_i^u$，其中
$\mathcal{D}_i^l$ 和 $\mathcal{D}_i^u$ 分别表示有标签数据和无标签数据。实际边缘智能任务中的数据形态并不单一，既可能来自图像类任务，也可能来自移动传感器和物联网时序任务。由于人工标注成本较高，有标签样本比例通常较低。

不同于传统联邦学习直接要求每个客户端执行本地模型训练，本文关注资源受限的边缘网络环境。在该环境中，客户端设备通常受限于算力、电量、无线带宽和连接稳定性，难以承担复杂模型的训练负载。因此，EdgeQGFed 将客户端视为轻量级数据参与者，将主要训练负载上移至边缘服务器，并由云端负责跨边缘节点的全局知识协调。该结构更符合真实边缘网络中客户端弱、边缘服务器相对强、云端具备全局协调能力的系统特征。

设 $f_\phi(\cdot)$ 表示特征编码器，$h_{\theta_e}(\cdot)$ 表示边缘服务器 $e$ 维护的分类器。对于图像类任务，$f_\phi$ 可由预训练视觉编码器实现；对于传感器时序任务，$f_\phi$ 可退化为恒等映射或轻量特征变换，由边缘分类器直接处理时序特征。本文中，编码器作为特征提取器使用，边缘分类器在训练过程中更新。云端维护一个教师分类器 $h_{\theta_c}(\cdot)$，并通过云端聚合机制从边缘模型中更新全局知识。

\subsection{网络约束下的学习目标}
\label{subsec:objective}

本文目标是在跨模态数据、统计异质性、标签稀缺、带宽受限和客户端参与不稳定的条件下训练高效模型。在第 $t$ 轮通信中，系统选择一组客户端
$\mathcal{S}_t\subseteq\mathcal{C}$ 参与训练，并由对应边缘服务器执行模型更新。客户端选择过程需要满足每轮通信预算约束：
\begin{equation}
    \sum_{i\in\mathcal{S}_t} m_i^{\mathrm{ce}}
    + \sum_{e\in\mathcal{E}_t} m_e^{\mathrm{ec}}
    + \sum_{e\in\mathcal{E}_t} m_e^{\mathrm{ceg}}
    \le B_t,
    \label{eq:budget}
\end{equation}
其中，$m_i^{\mathrm{ce}}$ 表示客户端到边缘服务器的上传数据量，可对应原始小批量数据或轻量特征；$m_e^{\mathrm{ec}}$ 表示边缘服务器到云端的上传数据量，包含模型更新与边缘摘要；$m_e^{\mathrm{ceg}}$ 表示云端向边缘服务器下发的数据量，$B_t$ 表示第 $t$ 轮的通信预算，$\mathcal{E}_t$ 表示该轮中的活跃边缘服务器集合。

为了刻画云-边-端协同训练的系统开销，本文将每轮训练时延建模为：
\begin{equation}
    T_t = T_t^{\mathrm{c2e}}
        + T_t^{\mathrm{edge}}
        + T_t^{\mathrm{e2c}}
        + T_t^{\mathrm{cloud}}
        + T_t^{\mathrm{c2e\_down}},
    \label{eq:latency}
\end{equation}
其中，$T_t^{\mathrm{c2e}}$ 表示端到边上传时延，$T_t^{\mathrm{edge}}$ 表示边缘计算时延，$T_t^{\mathrm{e2c}}$ 表示边到云上传时延，$T_t^{\mathrm{cloud}}$ 表示云端聚合时延，$T_t^{\mathrm{c2e\_down}}$ 表示云到边下发时延。该建模使本文能够从模型精度、通信开销和 time-to-target-accuracy 等角度综合评估分层协同学习系统。

\section{EdgeQGFed 方法}
\label{sec:method}

EdgeQGFed 是一种面向跨模态边缘智能任务的通信高效云-边-端协同学习框架。它主要包含三个核心组件：边缘侧半监督训练、云端质量感知图注意力聚合，以及网络约束下的资源感知客户端采样。为适应不同数据形态，EdgeQGFed 在数据增强、数据划分和聚合对象上采用模态自适应配置：图像任务使用图像增强与 Dirichlet 数据划分，传感器时序任务使用时序扰动和用户级划分；图像任务默认聚合可训练参数，时序任务可聚合完整模型状态以保留必要统计信息。

\subsection{边缘侧半监督训练}
\label{subsec:edge_training}

本文用 $x$ 表示任意边缘任务样本，其可以是图像样本，也可以是传感器时序样本。编码器 $f_\phi(\cdot)$ 将不同形态输入映射到特征空间，后续监督学习、一致性学习和原型计算均在该特征空间中进行。弱增强 $a_w(\cdot)$ 与强增强 $a_s(\cdot)$ 由数据形态决定：图像任务采用裁剪、旋转、颜色扰动等增强，数字类任务避免翻转操作，传感器时序任务采用噪声扰动、幅值缩放和时间遮挡。

在第 $t$ 轮训练中，每个活跃边缘服务器 $e$ 接收其覆盖范围内被选中客户端上传的数据批次。对于输入样本 $x$，编码器生成特征表示
$z=f_\phi(x)$，边缘分类器输出预测 logits $h_{\theta_e}(z)$。对于有标签样本，边缘服务器最小化监督交叉熵损失：
\begin{equation}
    \mathcal{L}_{\mathrm{sup}}^e =
    \frac{1}{|\mathcal{B}_e^l|}
    \sum_{(x,y)\in\mathcal{B}_e^l}
    \ell_{\mathrm{ce}}\left(h_{\theta_e}(f_\phi(x)), y\right),
    \label{eq:sup_loss}
\end{equation}
其中，$\mathcal{B}_e^l$ 表示边缘服务器 $e$ 当前批次中的有标签样本集合。

对于无标签样本，EdgeQGFed 利用云端教师模型和边缘模型之间的一致性进行学习。给定弱增强样本 $x^w=a_w(x)$ 和强增强样本 $x^s=a_s(x)$，边缘模型和云端模型的预测分别为：
\begin{equation}
    p_e = \mathrm{softmax}\left(h_{\theta_e}(f_\phi(x^s))/T_p\right), \quad
    p_c = \mathrm{softmax}\left(h_{\theta_c}(f_\phi(x^w))/T_p\right),
    \label{eq:edge_cloud_pred}
\end{equation}
其中，$T_p$ 表示伪标签温度。云边一致性损失定义为：
\begin{equation}
    \mathcal{L}_{\mathrm{con}}^e =
    d(p_e, p_c),
    \label{eq:consistency_loss}
\end{equation}
其中，$d(\cdot,\cdot)$ 可以采用均方误差、$\ell_1$ 距离或 KL 散度。实践中，一致性损失可进一步由置信度门控，只在云端与边缘预测较可靠的样本上计算，以避免早期低质量预测放大噪声。

为了降低低质量伪标签带来的确认偏差，EdgeQGFed 同时利用边缘置信度和云端置信度对伪标签进行加权。记
$\hat{y}_e=\arg\max p_e$，$\hat{y}_c=\arg\max p_c$，$q_e=\max p_e$，$q_c=\max p_c$。当边缘预测与云端预测一致时，该样本才参与伪标签训练，其权重定义为：
\begin{equation}
    w(x) =
    \mathbf{1}[\hat{y}_e=\hat{y}_c]
    \cdot
    \sigma\left(\frac{q_e-\tau_e}{\gamma}\right)
    \cdot
    \sigma\left(\frac{q_c-\tau_c}{\gamma}\right),
    \label{eq:pseudo_weight}
\end{equation}
其中，$\tau_e$ 和 $\tau_c$ 分别表示边缘置信度阈值和云端置信度阈值，$\gamma$ 表示权重平滑温度，$\sigma(\cdot)$ 表示 sigmoid 函数。加权伪标签损失为：
\begin{equation}
    \mathcal{L}_{\mathrm{pl}}^e =
    \frac{\sum_{x\in\mathcal{B}_e^u} w(x)
    \ell_{\mathrm{ce}}\left(h_{\theta_e}(f_\phi(x^s)), \hat{y}_c\right)}
    {\sum_{x\in\mathcal{B}_e^u} w(x)+\epsilon}.
    \label{eq:pseudo_loss}
\end{equation}

因此，边缘服务器 $e$ 的总体训练目标为：
\begin{equation}
    \mathcal{L}_e =
    \mathcal{L}_{\mathrm{sup}}^e
    + \lambda_t \mathcal{L}_{\mathrm{con}}^e
    + \lambda_p \mathcal{L}_{\mathrm{pl}}^e,
    \label{eq:edge_loss}
\end{equation}
其中，$\lambda_t$ 通过 warm-up 策略逐渐增大，$\lambda_p$ 控制伪标签损失权重。

\subsection{边缘摘要构建}
\label{subsec:summary}

边缘服务器完成本地更新后，会向云端上传质量感知摘要。该摘要不包含原始数据，而是由模型更新、类别原型、伪标签质量、有标签比例、预测置信度和批次规模组成：
\begin{equation}
    s_e = \{\theta_e, P_e, n_e, \rho_e, r_e, c_e, b_e\}.
    \label{eq:summary}
\end{equation}
其中，$\theta_e$ 表示边缘上传的聚合对象，可根据任务形态选取为可训练参数或完整模型状态；$P_e$ 表示类别原型矩阵，$n_e$ 表示每个类别对应的原型样本数，$\rho_e$ 表示平均伪标签权重，$r_e$ 表示有标签样本比例，$c_e$ 表示平均云端置信度，$b_e$ 表示该边缘节点处理的样本数。对于类别 $k$，其原型计算为：
\begin{equation}
    P_e^k =
    \frac{1}{|\mathcal{B}_{e,k}^l|}
    \sum_{(x,y)\in\mathcal{B}_{e,k}^l} f_\phi(x),
    \label{eq:prototype}
\end{equation}
其中，$\mathcal{B}_{e,k}^l$ 表示边缘服务器 $e$ 中属于类别 $k$ 的有标签样本集合。

\subsection{云端质量感知图注意力聚合}
\label{subsec:graph_aggregation}

本文将云端视为具有结构化知识建模能力的全局协调器，而不是简单的参数平均器。云端根据活跃边缘节点上传的摘要构建质量感知图，并通过图注意力机制实现跨边缘节点的选择性知识共享。该设计适用于不同数据形态下的边缘任务，因为图关系由模型表征、类别原型和训练质量信号共同决定，而不依赖某一特定输入模态。云端质量感知图注意力聚合的具体流程如算法~\ref{alg:graph_aggregation} 所示。

对于边缘节点 $e$，云端首先根据其模型参数计算模型签名 $u_e$。该签名由各层参数的均值、标准差和归一化范数等统计量拼接得到。同时，云端根据类别原型矩阵计算原型签名 $v_e$，即将按类别样本数加权后的原型矩阵展平并归一化。

对于两个活跃边缘节点 $i$ 和 $j$，云端计算其注意力得分：
\begin{align}
    a_{ij} =
    &\ \mathrm{cos}(u_i,u_j)
    + \alpha_p \mathrm{cos}(v_i,v_j)
    + \alpha_q (1-|\rho_i-\rho_j|) \nonumber \\
    &+ \alpha_r (1-|r_i-r_j|)
    + \alpha_c (1-|c_i-c_j|)
    + \beta \mathbf{1}[i=j],
    \label{eq:attention_score}
\end{align}
其中，$\alpha_p$、$\alpha_q$、$\alpha_r$ 和 $\alpha_c$ 分别控制类别原型相似性、伪标签可靠性、有标签比例相似性和预测置信度相似性的贡献，$\beta$ 表示自连接偏置，用于保留边缘节点自身的个性化知识。

随后，云端通过温度缩放 softmax 得到图注意力矩阵：
\begin{equation}
    A_{ij} =
    \frac{\exp(a_{ij}/\tau_g)}
    {\sum_{k\in\mathcal{E}_t}\exp(a_{ik}/\tau_g)},
    \label{eq:attention_matrix}
\end{equation}
其中，$\tau_g$ 表示图注意力温度。为避免注意力权重为零，本文对注意力矩阵设置最小权重下界，并进行行归一化。

基于注意力矩阵，云端为每个边缘节点生成个性化参数：
\begin{equation}
    \tilde{\theta}_i =
    \sum_{j\in\mathcal{E}_t} A_{ij}\theta_j.
    \label{eq:personalized_param}
\end{equation}
相比 FedAvg 对所有边缘更新进行统一平均，该方式能够使边缘节点更多吸收来自可靠且语义相似节点的知识，同时降低低质量或分布不匹配更新的影响。

云端全局参数进一步通过可靠性感知加权聚合得到：
\begin{equation}
    \bar{\theta} =
    \sum_{i\in\mathcal{E}_t}
    \omega_i \tilde{\theta}_i,
    \quad
    \omega_i =
    \frac{b_i(\rho_i+1)}
    {\sum_{j\in\mathcal{E}_t} b_j(\rho_j+1)}.
    \label{eq:global_param}
\end{equation}

最后，云端教师模型通过指数滑动平均更新：
\begin{equation}
    \theta_c \leftarrow
    \mu \theta_c + (1-\mu)\bar{\theta},
    \label{eq:ema}
\end{equation}
其中，$\mu$ 表示 AEMA 衰减系数。当采用 FedAvg 更新时，上式退化为直接用 $\bar{\theta}$ 替换云端教师参数。云端随后将个性化参数 $\tilde{\theta}_i$ 下发至对应边缘服务器；对于本轮未生成个性化参数的边缘节点，云端下发全局参数 $\bar{\theta}$ 作为回退同步，以避免边缘模型长期陈旧。

\begin{algorithm}[t]
\caption{云端质量感知图注意力聚合}
\label{alg:graph_aggregation}
\begin{algorithmic}[1]
\REQUIRE 活跃边缘摘要集合 $\mathcal{S}^{\mathrm{edge}}_t=\{s_e\}_{e\in\mathcal{E}_t}$，图温度 $\tau_g$，权重系数 $\alpha_p,\alpha_q,\alpha_r,\alpha_c$，自连接偏置 $\beta$，AEMA 衰减系数 $\mu$
\ENSURE 云端教师模型 $\theta_c$，个性化边缘参数集合 $\{\tilde{\theta}_e\}_{e\in\mathcal{E}_t}$
\FOR{each active edge $e\in\mathcal{E}_t$}
    \STATE 根据边缘参数 $\theta_e$ 提取模型签名 $u_e$
    \STATE 根据类别原型 $P_e$ 和原型计数 $n_e$ 提取原型签名 $v_e$
\ENDFOR
\FOR{each pair of active edges $(i,j)$}
    \STATE 根据式~\eqref{eq:attention_score} 计算质量感知注意力得分 $a_{ij}$
\ENDFOR
\STATE 根据式~\eqref{eq:attention_matrix} 对注意力得分进行 softmax 归一化，得到图注意力矩阵 $A$
\FOR{each target edge $i\in\mathcal{E}_t$}
    \STATE 根据式~\eqref{eq:personalized_param} 计算个性化参数 $\tilde{\theta}_i=\sum_{j\in\mathcal{E}_t}A_{ij}\theta_j$
\ENDFOR
\STATE 根据式~\eqref{eq:global_param} 计算可靠性感知全局参数 $\bar{\theta}$
\STATE 根据式~\eqref{eq:ema} 更新云端教师模型 $\theta_c\leftarrow\mu\theta_c+(1-\mu)\bar{\theta}$
\STATE 将个性化参数 $\{\tilde{\theta}_e\}_{e\in\mathcal{E}_t}$ 下发至对应边缘服务器，未个性化更新的边缘节点接收全局参数 $\bar{\theta}$
\end{algorithmic}
\end{algorithm}

\subsection{资源感知客户端采样}
\label{subsec:sampling}

为了提升通信效率，EdgeQGFed 在客户端选择阶段同时考虑数据价值和网络状态。对于客户端 $i$，定义资源感知效用分数：
\begin{equation}
    U_i =
    \frac{
    \eta_l l_i + \eta_d d_i + \eta_b b_i^{\mathrm{up}} + \eta_a a_i
    }
    {(T_i^{\mathrm{up}})^{\eta_t}},
    \label{eq:client_utility}
\end{equation}
其中，$l_i$ 表示客户端有标签比例，$d_i$ 表示归一化数据规模，$b_i^{\mathrm{up}}$ 表示归一化上行带宽，$a_i=1-p_i^{\mathrm{drop}}$ 表示可用性分数，$T_i^{\mathrm{up}}$ 表示估计上传时延。$\eta_l$、$\eta_d$、$\eta_b$、$\eta_a$ 和 $\eta_t$ 分别控制数据质量、数据规模、链路带宽、可用性和时延代价的重要性。

在每轮训练中，EdgeQGFed 优先选择效用较高的客户端，同时满足每个边缘服务器的最大并行上传数和式~\eqref{eq:budget} 中的通信预算约束。该策略能够避免将有限通信资源消耗在数据价值低、链路质量差或掉线概率高的客户端上，从而提高单位通信开销下的训练收益。

\subsection{整体训练流程}
\label{subsec:procedure}

EdgeQGFed 的整体训练流程如算法~\ref{alg:edgeqgfed} 所示。首先，系统根据任务形态确定增强、划分和聚合模式，并在通信预算和链路约束下执行资源感知客户端采样。被选中的活跃客户端将数据或特征上传至其关联边缘服务器。随后，边缘服务器基于监督损失、云边一致性损失和置信度加权伪标签损失更新本地分类器。接着，活跃边缘服务器向云端上传边缘摘要。云端根据摘要构建质量感知图，计算图注意力权重，生成边缘个性化参数，并通过 AEMA 或 FedAvg 更新云端教师模型。最后，云端将个性化参数或全局回退参数下发至边缘服务器，进入下一轮训练。

\begin{algorithm}[t]
\caption{EdgeQGFed 云-边-端协同训练流程}
\label{alg:edgeqgfed}
\begin{algorithmic}[1]
\REQUIRE 客户端集合 $\mathcal{C}$，边缘服务器集合 $\mathcal{E}$，云端教师模型 $\theta_c$，边缘模型集合 $\{\theta_e\}_{e\in\mathcal{E}}$，通信预算 $B_t$，总训练轮数 $T$
\ENSURE 云端教师模型 $\theta_c$，个性化边缘模型集合 $\{\theta_e\}_{e\in\mathcal{E}}$
\FOR{$t=1$ to $T$}
    \STATE 根据任务形态设置增强、数据划分和聚合对象
    \STATE 云端根据通信预算、链路带宽、掉线概率和数据质量执行资源感知客户端采样，得到 $\mathcal{S}_t$
    \FOR{each edge server $e\in\mathcal{E}$ in parallel}
        \STATE 获取与边缘 $e$ 关联的活跃客户端集合 $\mathcal{S}_{t,e}$
        \IF{$\mathcal{S}_{t,e}=\emptyset$}
            \STATE 跳过边缘服务器 $e$
        \ENDIF
        \STATE 客户端上传当前批次数据或特征至边缘服务器 $e$
        \STATE 边缘服务器利用式~\eqref{eq:sup_loss} 计算有标签监督损失 $\mathcal{L}_{\mathrm{sup}}^e$
        \STATE 边缘服务器利用式~\eqref{eq:consistency_loss} 计算云边一致性损失 $\mathcal{L}_{\mathrm{con}}^e$
        \STATE 根据式~\eqref{eq:pseudo_weight} 计算伪标签权重，并利用式~\eqref{eq:pseudo_loss} 计算伪标签损失 $\mathcal{L}_{\mathrm{pl}}^e$
        \STATE 通过式~\eqref{eq:edge_loss} 更新边缘模型参数 $\theta_e$
        \IF{当前轮满足云端聚合间隔}
            \STATE 构建边缘摘要 $s_e=\{\theta_e,P_e,n_e,\rho_e,r_e,c_e,b_e\}$ 并上传至云端
        \ENDIF
    \ENDFOR
    \IF{收到边缘摘要集合 $\mathcal{S}^{\mathrm{edge}}_t$}
        \STATE 云端调用算法~\ref{alg:graph_aggregation} 执行质量感知图注意力聚合
        \STATE 云端更新教师模型 $\theta_c$，并向边缘服务器下发个性化参数或全局回退参数
    \ENDIF
\ENDFOR
\end{algorithmic}
\end{algorithm}

通过结合分层训练负载分配、云端图注意力协同和网络感知客户端采样，EdgeQGFed 旨在解决跨模态边缘智能任务中的核心问题：在带宽受限、链路异构、标签稀缺和客户端参与不稳定条件下，实现更高效、更稳健的协同学习。

\section{实验评估}
\label{sec:evaluation}

本节从学习性能和网络系统效率两个角度评估 EdgeQGFed。与只关注最终分类精度的实验设置不同，本文同时报告通信开销、三层训练时延、time-to-accuracy、客户端不稳定参与下的鲁棒性，以及各个系统组件对整体性能的贡献。除非特别说明，所有方法均在相同的云-边-端拓扑、数据划分、通信预算和训练轮数下比较。本文报告训练过程中达到的最佳测试性能，并记录达到该性能时对应的通信开销和估计时延。

\subsection{实验设置}
\label{subsec:exp_setup}

\textbf{数据集与任务。}
本文使用四个公开数据集进行评估，覆盖传感器时序任务和图像类任务。HARBox 作为主要边缘智能数据集，其用户级传感器记录能够体现真实客户端差异。SVHN、CIFAR-10 和 CIFAR-100 用于验证 EdgeQGFed 在图像任务上的泛化能力，其中 CIFAR-100 用于评估类别数较多时的训练稳定性。对于 HARBox，本文按照用户进行数据划分；对于 SVHN、CIFAR-10 和 CIFAR-100，本文采用 Dirichlet non-IID 划分，并重点报告 $\alpha=0.3$ 和 $\alpha=0.1$ 下的结果。

\begin{table}[t]
\centering
\caption{数据集与默认划分设置}
\label{tab:datasets}
\begin{tabular}{lcccc}
\toprule
数据集 & 数据形态 & 类别数 & 划分方式 & 标签比例 \\
\midrule
HARBox & 传感器时序 & 5 & 用户级划分 & 0.1 \\
SVHN & 图像 & 10 & Dirichlet & 0.1 \\
CIFAR-10 & 图像 & 10 & Dirichlet & 0.1 \\
CIFAR-100 & 图像 & 100 & Dirichlet & 0.1 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{系统配置。}
默认系统包含一个云服务器、多个边缘服务器和若干客户端。图像数据集默认设置为 4 个边缘服务器和 128 个客户端；HARBox 使用用户级客户端划分。每轮训练中，云端根据采样策略选择部分客户端，活跃客户端将当前批次数据或特征上传到对应边缘服务器。默认链路设置采用异构 edge profile，并在后续实验中进一步改变带宽、通信预算和客户端掉线率。

\textbf{对比方法。}
本文比较 EdgeQGFed 与多种分层联邦学习基线。H-FedAvg 表示不使用图注意力和伪标签学习的基础分层 FedAvg。H-FedAvg+PL 在 H-FedAvg 上加入伪标签训练。EdgeQGFed w/o Graph 去除云端图注意力聚合，改用加权参数平均。EdgeQGFed w/o RS 将资源感知采样替换为均衡采样。EdgeQGFed w/o PL 去除伪标签损失，仅保留监督训练和云边一致性训练。完整 EdgeQGFed 同时使用资源感知客户端采样、边缘侧半监督训练和云端质量感知图注意力聚合。

\textbf{评价指标。}
学习性能使用云端准确率和边缘平均准确率衡量。系统效率使用总通信量、累计通信量、形式化轮时延、累计估计时延和 time-to-target accuracy 衡量。为解释系统行为，本文还记录选中客户端数、活跃客户端数、客户端掉线率、伪标签比例、伪标签权重和图注意力统计量。

\subsection{总体性能}
\label{subsec:overall_performance}

首先在默认网络设置下比较 EdgeQGFed 与各类基线方法。该实验用于回答 EdgeQGFed 是否能够在保持通信效率的同时提升模型性能。表~\ref{tab:overall_performance} 报告云端准确率、边缘平均准确率、损失、time-to-target 和总通信量。图~\ref{fig:overall} 展示随通信轮数和累计通信量变化的收敛曲线。

\begin{table*}[t]
\centering
\caption{默认边缘网络条件下的总体性能}
\label{tab:overall_performance}
\begin{tabular}{llcccccc}
\toprule
数据集 & 方法 & 云端准确率 & 边缘准确率 & 云端损失 & 边缘损失 & Time-to-Target & 通信量 \\
\midrule
HARBox & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
HARBox & H-FedAvg+PL & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
HARBox & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
SVHN & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
SVHN & H-FedAvg+PL & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
SVHN & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-10 & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-10 & H-FedAvg+PL & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-10 & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-100 & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-100 & H-FedAvg+PL & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-100 & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
\bottomrule
\end{tabular}
\end{table*}

\textbf{绘图方式。}
图~\ref{fig:overall}(a) 为折线图，横轴为通信轮数，纵轴为云端准确率；图~\ref{fig:overall}(b) 为折线图，横轴为累计通信量，纵轴为云端准确率；图~\ref{fig:overall}(c) 为分组柱状图，横轴为数据集，纵轴为 time-to-target，不同柱表示不同方法。

\subsection{通信效率}
\label{subsec:comm_efficiency}

随后评估不同通信预算下的训练效率。该实验更贴近 INFOCOM 关注的网络系统问题，即在有限通信资源下，方法是否能够更快达到目标精度。本文改变每轮通信预算，并比较 EdgeQGFed、H-FedAvg、H-FedAvg+PL 和 EdgeQGFed w/o RS。

\begin{table}[t]
\centering
\caption{通信预算敏感性}
\label{tab:budget}
\begin{tabular}{llcccc}
\toprule
数据集 & 预算等级 & 方法 & 云端准确率 & Time-to-Target & 预算使用率 \\
\midrule
HARBox & Low & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
HARBox & Low & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-10 & Medium & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-10 & Medium & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-100 & High & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-100 & High & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{绘图方式。}
主要图为折线图，横轴为累计通信量，纵轴为云端准确率。另一张折线图以每轮通信预算为横轴，以 time-to-target 为纵轴，用于展示 EdgeQGFed 是否能够更有效地利用通信资源。

\subsection{时延与 Time-to-Accuracy}
\label{subsec:latency}

边缘智能系统不能只比较精度，还需要比较达到目标精度所需的系统时延。本文基于式~\eqref{eq:latency} 评估三层时延模型，分别报告端到边上传、边缘计算、边到云上传、云端聚合和云到边下发五个部分。进一步地，本文记录 time-to-target accuracy，用于衡量在相同网络条件下模型达到可用精度的速度。

\begin{table}[t]
\centering
\caption{默认网络条件下的时延分解}
\label{tab:latency}
\begin{tabular}{lccccc}
\toprule
方法 & 端到边 & 边缘计算 & 边到云 & 云端聚合 & 云到边 \\
\midrule
H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
EdgeQGFed w/o RS & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{绘图方式。}
时延分解采用堆叠柱状图，横轴为方法或数据集，纵轴为时延。收敛效率采用折线图，横轴为累计估计时延，纵轴为云端准确率。

\subsection{链路异构与客户端移动性}
\label{subsec:heterogeneity}

本文进一步评估带宽异构和客户端不稳定参与下的鲁棒性。对于带宽异构，本文设置不同强度的 edge profile，并测试随机带宽分布；对于客户端移动性，本文改变客户端掉线概率，并可引入切换导致的额外掉线风险。该实验用于验证资源感知采样能否避开低质量链路客户端，以及图注意力聚合能否在边缘更新不完整时保持稳定。

\begin{table}[t]
\centering
\caption{客户端掉线和移动性下的鲁棒性}
\label{tab:dropout}
\begin{tabular}{lcccc}
\toprule
掉线率 & 方法 & 云端准确率 & 活跃客户端数 & Time-to-Target \\
\midrule
0.0 & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
0.0 & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
0.1 & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
0.1 & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
0.3 & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
0.3 & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{绘图方式。}
带宽异构实验采用折线图，横轴为异构程度，纵轴为 time-to-target 或最终云端准确率。客户端移动性实验采用折线图，横轴为掉线率，纵轴为云端准确率。

\subsection{消融实验}
\label{subsec:ablation}

为了分析各组件的作用，本文分别去除图注意力聚合、资源感知采样、伪标签学习和云边一致性训练。默认在 HARBox 和 CIFAR-10 上进行消融，必要时在 CIFAR-100 上补充难分类场景结果。预期结果是，图注意力聚合能够缓解 non-IID 下的跨边缘负迁移，资源感知采样能够降低 time-to-target，伪标签学习能够在少标签条件下提升模型性能。

\begin{table}[t]
\centering
\caption{消融实验}
\label{tab:ablation}
\begin{tabular}{lcccc}
\toprule
变体 & 图聚合 & 资源采样 & 伪标签 & 云端准确率 \\
\midrule
完整 EdgeQGFed & 是 & 是 & 是 & \rule{0.8cm}{0.4pt} \\
w/o Graph & 否 & 是 & 是 & \rule{0.8cm}{0.4pt} \\
w/o RS & 是 & 否 & 是 & \rule{0.8cm}{0.4pt} \\
w/o PL & 是 & 是 & 否 & \rule{0.8cm}{0.4pt} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{绘图方式。}
消融实验采用分组柱状图，横轴为不同变体，纵轴分别为云端准确率、time-to-target 和通信开销。该图应放在总体性能和通信效率结果之后，用于解释 EdgeQGFed 的性能来源。

\subsection{敏感性分析}
\label{subsec:sensitivity}

最后，本文分析 EdgeQGFed 对数据异构程度和标签比例的敏感性。对于 SVHN、CIFAR-10 和 CIFAR-100，本文改变 Dirichlet 参数 $\alpha\in\{0.1,0.3,0.5,1.0\}$。对于弱标签监督，本文改变有标签比例 $\{0.05,0.1,0.2,1.0\}$。该实验用于说明 EdgeQGFed 在更强 non-IID 和更少标签条件下是否仍然有效。

\textbf{绘图方式。}
non-IID 敏感性实验采用折线图，横轴为 Dirichlet 参数，纵轴为云端准确率。标签比例敏感性实验采用折线图，横轴为有标签比例，纵轴为云端准确率或边缘平均准确率。曲线包含 H-FedAvg、H-FedAvg+PL 和 EdgeQGFed。
