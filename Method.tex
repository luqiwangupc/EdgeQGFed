\section{Preliminaries and Problem Formulation}
\label{sec:problem}

\subsection{Hierarchical Edge Learning System}
\label{subsec:system}

We consider a cloud-edge-client learning system with one cloud server, a set of edge servers
$\mathcal{E}=\{1,\ldots,E\}$, and a set of clients $\mathcal{C}=\{1,\ldots,C\}$. Each client
$i$ is associated with one edge server $e(i)\in\mathcal{E}$. Client $i$ owns a local dataset
$\mathcal{D}_i=\mathcal{D}_i^l\cup\mathcal{D}_i^u$, where $\mathcal{D}_i^l$ and
$\mathcal{D}_i^u$ denote labeled and unlabeled samples, respectively. The labeled ratio is
usually small in practical edge intelligence tasks because data annotation is expensive, while
unlabeled data are continuously generated by distributed devices.

Unlike conventional federated learning where each client directly trains a local model, we
target resource-constrained edge networks in which clients are lightweight data participants.
The major training workload is moved from clients to edge servers. Each selected client uploads
its mini-batch data or features to its associated edge server, and the edge server performs local
model updates. The cloud server coordinates cross-edge knowledge sharing. This design matches
edge environments where clients have limited computation, energy, and wireless connectivity,
while edge servers and the cloud have stronger computation and more stable communication links.

Let $f_\phi(\cdot)$ denote a feature encoder and $h_{\theta_e}(\cdot)$ denote the classifier
maintained by edge server $e$. For image tasks, $f_\phi$ can be instantiated by a pretrained
visual encoder. For sensor time-series tasks, $f_\phi$ can be an identity mapping or a
lightweight transformation, while the edge classifier directly handles temporal features. In
this work, the encoder is used as a feature extractor and the edge classifier is updated during
training. The cloud maintains a teacher classifier $h_{\theta_c}(\cdot)$, which is updated from
edge models through cloud-side aggregation.

\subsection{Learning Objective Under Network Constraints}
\label{subsec:objective}

The goal is to train an accurate model under data heterogeneity, label scarcity, bandwidth
constraints, and client instability. At each communication round $t$, the system selects a
subset of clients $\mathcal{S}_t\subseteq\mathcal{C}$ and trains edge models with their data.
The client selection must satisfy a round communication budget:
\begin{equation}
    \sum_{i\in\mathcal{S}_t} m_i^{\mathrm{ce}}
    + \sum_{e\in\mathcal{E}_t} m_e^{\mathrm{ec}}
    + \sum_{e\in\mathcal{E}_t} m_e^{\mathrm{ceg}}
    \le B_t,
    \label{eq:budget}
\end{equation}
where $m_i^{\mathrm{ce}}$ is the client-to-edge upload size, corresponding to either raw
mini-batch data or lightweight features, $m_e^{\mathrm{ec}}$ is the edge-to-cloud upload size
including model updates and edge summaries, $m_e^{\mathrm{ceg}}$ is the cloud-to-edge downlink
size, and $B_t$ is the communication budget of round $t$. $\mathcal{E}_t$ denotes the set of
active edge servers in round $t$.

We also model the round latency as a three-level cloud-edge-client process:
\begin{equation}
    T_t = T_t^{\mathrm{c2e}}
        + T_t^{\mathrm{edge}}
        + T_t^{\mathrm{e2c}}
        + T_t^{\mathrm{cloud}}
        + T_t^{\mathrm{c2e\_down}},
    \label{eq:latency}
\end{equation}
where $T_t^{\mathrm{c2e}}$ is the terminal-to-edge upload latency, $T_t^{\mathrm{edge}}$ is
the edge computation time, $T_t^{\mathrm{e2c}}$ is the edge-to-cloud upload latency,
$T_t^{\mathrm{cloud}}$ is the cloud aggregation time, and $T_t^{\mathrm{c2e\_down}}$ is the
cloud-to-edge downlink latency. This formulation allows us to evaluate not only accuracy but
also communication cost and time-to-target-accuracy.

\section{The Proposed EdgeQGFed Framework}
\label{sec:method}

EdgeQGFed is a communication-efficient cloud-edge-client collaborative learning framework. It
contains three key components: edge-side semi-supervised training, cloud-side quality-aware graph
attention aggregation, and resource-aware client sampling under network constraints. To support
different data modalities, EdgeQGFed adopts modality-aware configurations for augmentation,
partitioning, and aggregation payloads. Image tasks use image augmentations and Dirichlet
partitions, while sensor time-series tasks use temporal perturbations and user-level partitions.
For image tasks, EdgeQGFed aggregates trainable parameters by default; for sensor tasks, it can
aggregate the full model state when preserving task-specific statistics is beneficial.

\subsection{Edge-Side Semi-Supervised Training}
\label{subsec:edge_training}

At round $t$, each active edge server $e$ receives mini-batches from selected active clients
under its coverage. For an input $x$, the encoder produces a representation
$z=f_\phi(x)$, and the edge classifier outputs logits $h_{\theta_e}(z)$. For labeled samples,
the edge server minimizes the supervised cross-entropy loss:
\begin{equation}
    \mathcal{L}_{\mathrm{sup}}^e =
    \frac{1}{|\mathcal{B}_e^l|}
    \sum_{(x,y)\in\mathcal{B}_e^l}
    \ell_{\mathrm{ce}}\left(h_{\theta_e}(f_\phi(x)), y\right),
    \label{eq:sup_loss}
\end{equation}
where $\mathcal{B}_e^l$ is the labeled batch at edge $e$.

For unlabeled samples, EdgeQGFed uses cloud-edge consistency. Given a weakly augmented input
$x^w=a_w(x)$ and a strongly augmented input $x^s=a_s(x)$, the edge prediction and cloud
prediction are:
\begin{equation}
    p_e = \mathrm{softmax}\left(h_{\theta_e}(f_\phi(x^s))/T_p\right), \quad
    p_c = \mathrm{softmax}\left(h_{\theta_c}(f_\phi(x^w))/T_p\right),
    \label{eq:edge_cloud_pred}
\end{equation}
where $T_p$ is the pseudo-label temperature. The consistency loss is:
\begin{equation}
    \mathcal{L}_{\mathrm{con}}^e =
    d(p_e, p_c),
    \label{eq:consistency_loss}
\end{equation}
where $d(\cdot,\cdot)$ can be mean squared error, $\ell_1$ distance, or KL divergence.
The weak and strong augmentations are modality-aware: image tasks use cropping, rotation, and
color perturbation; digit-oriented tasks avoid flipping; sensor time-series tasks use noise,
scaling, and temporal masking. In practice, the consistency term can also be gated by
confidence so that early unreliable predictions do not dominate edge training.

To reduce confirmation bias, pseudo-labels are weighted by both edge confidence and cloud
confidence. Let $\hat{y}_e=\arg\max p_e$, $\hat{y}_c=\arg\max p_c$, $q_e=\max p_e$, and
$q_c=\max p_c$. A sample is used for pseudo-label training only when the edge and cloud
predictions agree. Its soft weight is:
\begin{equation}
    w(x) =
    \mathbf{1}[\hat{y}_e=\hat{y}_c]
    \cdot
    \sigma\left(\frac{q_e-\tau_e}{\gamma}\right)
    \cdot
    \sigma\left(\frac{q_c-\tau_c}{\gamma}\right),
    \label{eq:pseudo_weight}
\end{equation}
where $\tau_e$ and $\tau_c$ are edge and cloud confidence thresholds, $\gamma$ is a temperature
for weight smoothing, and $\sigma(\cdot)$ is the sigmoid function. The weighted pseudo-label
loss is:
\begin{equation}
    \mathcal{L}_{\mathrm{pl}}^e =
    \frac{\sum_{x\in\mathcal{B}_e^u} w(x)
    \ell_{\mathrm{ce}}\left(h_{\theta_e}(f_\phi(x^s)), \hat{y}_c\right)}
    {\sum_{x\in\mathcal{B}_e^u} w(x)+\epsilon}.
    \label{eq:pseudo_loss}
\end{equation}

The overall edge training objective is:
\begin{equation}
    \mathcal{L}_e =
    \mathcal{L}_{\mathrm{sup}}^e
    + \lambda_t \mathcal{L}_{\mathrm{con}}^e
    + \lambda_p \mathcal{L}_{\mathrm{pl}}^e,
    \label{eq:edge_loss}
\end{equation}
where $\lambda_t$ is gradually increased by a ramp-up schedule and $\lambda_p$ controls the
pseudo-label loss.

\subsection{Edge Summary Construction}
\label{subsec:summary}

After local edge training, each active edge server uploads a compact summary to the cloud. The
summary contains model updates, class prototypes, pseudo-label quality, labeled ratio,
prediction confidence, and batch size:
\begin{equation}
    s_e = \{\theta_e, P_e, n_e, \rho_e, r_e, c_e, b_e\}.
    \label{eq:summary}
\end{equation}
Here $\theta_e$ denotes the aggregation payload of edge $e$, which can be trainable parameters
or the full model state depending on the task modality. $P_e$ is the class prototype matrix,
$n_e$ is the class-wise prototype count, $\rho_e$ is the average pseudo-label weight, $r_e$ is
the labeled ratio, $c_e$ is the mean cloud confidence, and $b_e$ is the number of processed
samples. For class $k$, the prototype is computed as:
\begin{equation}
    P_e^k =
    \frac{1}{|\mathcal{B}_{e,k}^l|}
    \sum_{(x,y)\in\mathcal{B}_{e,k}^l} f_\phi(x),
    \label{eq:prototype}
\end{equation}
where $\mathcal{B}_{e,k}^l$ is the labeled subset of class $k$ at edge $e$.

\subsection{Cloud-Side Quality-Aware Graph Attention Aggregation}
\label{subsec:graph_aggregation}

The cloud is not treated as a passive parameter averager. Instead, it constructs a
quality-aware graph over active edge servers and performs graph attention aggregation. For edge
$e$, we first compute a model signature $u_e$ from its parameters by concatenating layer-wise
statistics such as mean, standard deviation, and normalized norm. We also compute a prototype
signature $v_e$ by flattening and normalizing the count-weighted prototype matrix.

For two active edges $i$ and $j$, the cloud computes their attention score as:
\begin{align}
    a_{ij} =
    &\ \mathrm{cos}(u_i,u_j)
    + \alpha_p \mathrm{cos}(v_i,v_j)
    + \alpha_q (1-|\rho_i-\rho_j|) \nonumber \\
    &+ \alpha_r (1-|r_i-r_j|)
    + \alpha_c (1-|c_i-c_j|)
    + \beta \mathbf{1}[i=j],
    \label{eq:attention_score}
\end{align}
where $\alpha_p$, $\alpha_q$, $\alpha_r$, and $\alpha_c$ control the contributions of prototype
similarity, pseudo-label reliability, labeled ratio similarity, and confidence similarity.
$\beta$ is a self-bias term that preserves edge-specific knowledge.

The attention matrix is obtained by temperature-scaled softmax:
\begin{equation}
    A_{ij} =
    \frac{\exp(a_{ij}/\tau_g)}
    {\sum_{k\in\mathcal{E}_t}\exp(a_{ik}/\tau_g)},
    \label{eq:attention_matrix}
\end{equation}
where $\tau_g$ is the graph temperature. A small lower bound is applied to avoid zero attention,
followed by row-wise renormalization.

The cloud generates personalized parameters for each edge:
\begin{equation}
    \tilde{\theta}_i =
    \sum_{j\in\mathcal{E}_t} A_{ij}\theta_j.
    \label{eq:personalized_param}
\end{equation}
Compared with FedAvg, this aggregation allows edge $i$ to receive more knowledge from reliable
and semantically similar edges, while reducing the influence of low-quality or mismatched
updates.

The global cloud parameter is then computed by reliability-aware weighted aggregation:
\begin{equation}
    \bar{\theta} =
    \sum_{i\in\mathcal{E}_t}
    \omega_i \tilde{\theta}_i,
    \quad
    \omega_i =
    \frac{b_i(\rho_i+1)}
    {\sum_{j\in\mathcal{E}_t} b_j(\rho_j+1)}.
    \label{eq:global_param}
\end{equation}

Finally, the cloud teacher is updated by adaptive exponential moving average:
\begin{equation}
    \theta_c \leftarrow
    \mu \theta_c + (1-\mu)\bar{\theta},
    \label{eq:ema}
\end{equation}
where $\mu$ is the EMA decay. When FedAvg is used, the update degenerates to directly replacing
the cloud teacher with $\bar{\theta}$. The personalized parameters $\tilde{\theta}_i$ are sent
back to edge servers for the next round. If an edge does not receive a personalized update in
the current round, the global parameter $\bar{\theta}$ is used as a fallback synchronization.

\subsection{Resource-Aware Client Sampling}
\label{subsec:sampling}

To improve communication efficiency, EdgeQGFed selects clients according to both data utility
and network conditions. For client $i$, we define a resource-aware utility score:
\begin{equation}
    U_i =
    \frac{
    \eta_l l_i + \eta_d d_i + \eta_b b_i^{\mathrm{up}} + \eta_a a_i
    }
    {(T_i^{\mathrm{up}})^{\eta_t}},
    \label{eq:client_utility}
\end{equation}
where $l_i$ is the labeled ratio, $d_i$ is the normalized data size,
$b_i^{\mathrm{up}}$ is the normalized uplink bandwidth, $a_i=1-p_i^{\mathrm{drop}}$ is the
availability score, and $T_i^{\mathrm{up}}$ is the estimated upload latency. The coefficients
$\eta_l$, $\eta_d$, $\eta_b$, $\eta_a$, and $\eta_t$ control the importance of data quality,
data quantity, bandwidth, availability, and latency cost.

At each round, EdgeQGFed selects high-utility clients while respecting the maximum number of
parallel uploads per edge and the round communication budget in Eq.~\eqref{eq:budget}. This
sampling strategy avoids wasting communication resources on clients with low data value, poor
links, or high dropout probability.

\subsection{Overall Training Procedure}
\label{subsec:procedure}

The overall training process is as follows. The system first determines the modality-aware
augmentation, partitioning, and aggregation mode. In each round, it performs resource-aware
client sampling under communication constraints. Active clients upload data or features to
their associated edge servers. Each edge server updates its classifier using
supervised loss, cloud-edge consistency loss, and confidence-aware pseudo-label loss. Then,
active edges upload summaries to the cloud. The cloud constructs a quality-aware graph, computes
attention-based personalized parameters, updates the cloud teacher by EMA or FedAvg, and sends
personalized or fallback global parameters back to edges. This loop continues until the total
number of training rounds is reached.

By combining hierarchical workload placement, graph-based cloud coordination, and
network-aware sampling, EdgeQGFed targets the key challenge of edge intelligence: reaching high
accuracy under limited bandwidth, heterogeneous links, label scarcity, and unstable client
participation.

\section{Experimental Evaluation}
\label{sec:evaluation}

This section evaluates EdgeQGFed from both learning and networking perspectives. Following the
evaluation style of edge intelligence and networked learning systems, we do not only compare
final accuracy. We also report communication cost, latency, time-to-accuracy, robustness under
unstable client participation, and the contribution of each system component. Unless otherwise
specified, all methods are evaluated under the same cloud-edge-client topology, data partition,
communication budget, and training schedule. We report the best test performance achieved
during training and record the corresponding communication and latency cost.

\subsection{Experimental Setup}
\label{subsec:exp_setup}

\textbf{Datasets and tasks.}
We evaluate EdgeQGFed on four public datasets that cover both sensor time-series and image
tasks. HARBox is used as the main edge intelligence benchmark because its user-level sensor
records naturally reflect client heterogeneity. SVHN, CIFAR-10, and CIFAR-100 are used to
evaluate the generality of the framework on image-based learning tasks with different numbers
of classes and difficulty levels. For HARBox, we partition data by users to emulate realistic
client ownership. For SVHN, CIFAR-10, and CIFAR-100, we use Dirichlet non-IID partitions and
report results under $\alpha=0.3$ and $\alpha=0.1$.

\begin{table}[t]
\centering
\caption{Datasets and default partition settings.}
\label{tab:datasets}
\begin{tabular}{lcccc}
\toprule
Dataset & Modality & Classes & Partition & Labeled ratio \\
\midrule
HARBox & Sensor time-series & 5 & User-level & 0.1 \\
SVHN & Image & 10 & Dirichlet & 0.1 \\
CIFAR-10 & Image & 10 & Dirichlet & 0.1 \\
CIFAR-100 & Image & 100 & Dirichlet & 0.1 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{System configuration.}
The default topology contains one cloud server, multiple edge servers, and a set of clients.
Each client is associated with one edge server. Unless otherwise stated, image datasets use
4 edge servers and 128 clients, while HARBox uses user-level clients. In each round, the cloud
selects a subset of clients according to the sampling policy, and active clients upload their
mini-batch data or features to the corresponding edge server. The default bandwidth setting
uses heterogeneous edge profiles. We further vary bandwidth, communication budget, and client
dropout in dedicated experiments.

\textbf{Compared methods.}
We compare EdgeQGFed with hierarchical and semi-supervised federated baselines. H-FedAvg
performs hierarchical FedAvg without graph modeling or pseudo-label learning. H-FedAvg+PL adds
pseudo-label training to H-FedAvg. EdgeQGFed w/o Graph removes cloud-side graph attention and
uses weighted parameter averaging. EdgeQGFed w/o RS replaces resource-aware sampling with
balanced sampling. EdgeQGFed w/o PL removes the pseudo-label loss while keeping supervised and
consistency training. The full EdgeQGFed uses resource-aware sampling, edge-side
semi-supervised training, and cloud-side quality-aware graph attention aggregation.

\textbf{Metrics.}
We use cloud accuracy and edge-average accuracy to evaluate learning performance. We use total
communication, cumulative communication, formal round latency, cumulative estimated latency,
and time-to-target accuracy to evaluate network efficiency. We also report selected clients,
active clients, client drop ratio, pseudo-label ratio, pseudo-label weight, and graph attention
statistics to explain the behavior of the system.

\subsection{Overall Performance}
\label{subsec:overall_performance}

We first compare EdgeQGFed with all baselines under the default network setting. This experiment
answers whether EdgeQGFed can improve learning performance without sacrificing communication
efficiency. Table~\ref{tab:overall_performance} reports cloud accuracy, edge-average accuracy,
loss, time-to-target accuracy, and total communication. Figure~\ref{fig:overall} plots the
accuracy curves over communication rounds and cumulative communication.

\begin{table*}[t]
\centering
\caption{Overall performance under the default edge network setting.}
\label{tab:overall_performance}
\begin{tabular}{llcccccc}
\toprule
Dataset & Method & Cloud Acc. & Edge Acc. & Cloud Loss & Edge Loss & Time-to-Target & Comm. \\
\midrule
HARBox & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
HARBox & H-FedAvg+PL & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
HARBox & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
SVHN & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
SVHN & H-FedAvg+PL & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
SVHN & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-10 & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-10 & H-FedAvg+PL & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-10 & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-100 & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-100 & H-FedAvg+PL & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-100 & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
\bottomrule
\end{tabular}
\end{table*}

\textbf{Figure design.}
Figure~\ref{fig:overall}(a) is a line chart with communication rounds on the x-axis and cloud
accuracy on the y-axis. Figure~\ref{fig:overall}(b) uses cumulative communication as the x-axis
and cloud accuracy as the y-axis. Figure~\ref{fig:overall}(c) is a grouped bar chart comparing
time-to-target accuracy across methods and datasets.

\subsection{Communication Efficiency}
\label{subsec:comm_efficiency}

We next evaluate EdgeQGFed under different communication budgets. This experiment is central
to networked edge learning because the best method should reach high accuracy with fewer
uploaded bytes and fewer ineffective rounds. We vary the round communication budget and compare
EdgeQGFed with H-FedAvg, H-FedAvg+PL, and EdgeQGFed w/o RS.

\begin{table}[t]
\centering
\caption{Communication budget sensitivity.}
\label{tab:budget}
\begin{tabular}{llcccc}
\toprule
Dataset & Budget & Method & Cloud Acc. & Time-to-Target & Budget Used \\
\midrule
HARBox & Low & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
HARBox & Low & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-10 & Medium & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-10 & Medium & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-100 & High & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
CIFAR-100 & High & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Figure design.}
The main figure is a line chart with cumulative communication on the x-axis and cloud accuracy
on the y-axis. A second line chart uses round communication budget on the x-axis and
time-to-target accuracy on the y-axis. These figures show whether EdgeQGFed converts limited
communication resources into model improvement more efficiently.

\subsection{Latency and Time-to-Accuracy}
\label{subsec:latency}

Accuracy alone is insufficient for edge intelligence systems. We therefore evaluate the
three-layer latency model defined in Eq.~\eqref{eq:latency}. For each method, we report the
latency components from client-to-edge upload, edge computation, edge-to-cloud upload, cloud
aggregation, and cloud-to-edge downlink. We further report time-to-target accuracy, which
measures how quickly a method reaches a practical accuracy target under the same network
condition.

\begin{table}[t]
\centering
\caption{Latency breakdown under the default network setting.}
\label{tab:latency}
\begin{tabular}{lccccc}
\toprule
Method & C2E & Edge & E2C & Cloud & C2E-Down \\
\midrule
H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
EdgeQGFed w/o RS & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Figure design.}
The latency breakdown is plotted as a stacked bar chart. The x-axis is the method or dataset,
and the y-axis is latency in seconds. Another line chart uses cumulative estimated latency on
the x-axis and cloud accuracy on the y-axis.

\subsection{Impact of Network Heterogeneity and Client Mobility}
\label{subsec:heterogeneity}

We evaluate robustness under heterogeneous bandwidth and unstable participation. For bandwidth
heterogeneity, we test deterministic edge profiles and sampled bandwidth distributions. For
client mobility, we vary the drop probability and optionally add handoff-induced drop boosts.
This experiment tests whether resource-aware sampling can avoid clients with poor links and
whether graph aggregation can remain stable when edge updates are incomplete.

\begin{table}[t]
\centering
\caption{Robustness under client dropout and mobility.}
\label{tab:dropout}
\begin{tabular}{lcccc}
\toprule
Drop Rate & Method & Cloud Acc. & Active Clients & Time-to-Target \\
\midrule
0.0 & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
0.0 & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
0.1 & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
0.1 & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
0.3 & H-FedAvg & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
0.3 & EdgeQGFed & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} & \rule{0.7cm}{0.4pt} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Figure design.}
For bandwidth heterogeneity, we use line charts with heterogeneity level on the x-axis and
time-to-target accuracy or final cloud accuracy on the y-axis. For mobility, we use line charts
with client drop rate on the x-axis and cloud accuracy on the y-axis.

\subsection{Ablation Study}
\label{subsec:ablation}

We isolate the contribution of each component by removing graph attention aggregation,
resource-aware sampling, pseudo-label learning, and cloud-edge consistency. The ablation study
is conducted on HARBox and CIFAR-10 by default, and can be extended to CIFAR-100 for the
hard-class setting. The expected result is that graph attention improves cross-edge knowledge
sharing under non-IID data, resource-aware sampling reduces time-to-accuracy, and pseudo-label
learning improves performance when labels are scarce.

\begin{table}[t]
\centering
\caption{Ablation study.}
\label{tab:ablation}
\begin{tabular}{lcccc}
\toprule
Variant & Graph & RS & PL & Cloud Acc. \\
\midrule
Full EdgeQGFed & \checkmark & \checkmark & \checkmark & \rule{0.8cm}{0.4pt} \\
w/o Graph & -- & \checkmark & \checkmark & \rule{0.8cm}{0.4pt} \\
w/o RS & \checkmark & -- & \checkmark & \rule{0.8cm}{0.4pt} \\
w/o PL & \checkmark & \checkmark & -- & \rule{0.8cm}{0.4pt} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Figure design.}
We use a grouped bar chart with model variants on the x-axis and cloud accuracy,
time-to-target accuracy, and communication cost on the y-axis. This figure should be placed
after the main performance and efficiency results.

\subsection{Sensitivity Analysis}
\label{subsec:sensitivity}

Finally, we study how EdgeQGFed behaves under different non-IID levels and label ratios. For
SVHN, CIFAR-10, and CIFAR-100, we vary the Dirichlet parameter
$\alpha\in\{0.1,0.3,0.5,1.0\}$. For weak supervision, we vary the labeled ratio in
$\{0.05,0.1,0.2,1.0\}$. These experiments show whether EdgeQGFed remains effective when data
heterogeneity becomes stronger or labeled data become scarce.

\textbf{Figure design.}
For non-IID sensitivity, the x-axis is the Dirichlet parameter and the y-axis is cloud
accuracy. For label-ratio sensitivity, the x-axis is labeled ratio and the y-axis is cloud
accuracy or edge-average accuracy. Curves include H-FedAvg, H-FedAvg+PL, and EdgeQGFed.
